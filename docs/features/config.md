
## Config features

The config feature allow user to view and set config value in the webapp.
 
the config can be get/set via packages/app/src/db/clients/config.ts

### Pages

There are 1 page:
- Search home page `/config`
 user can edit/create/delete config, also search config via string (frontend search, match keyname and description)